{
  "name": "la-belle-bretagne",
  "version": "1.0.0",
  "description": "Site touristique interactif pour découvrir la Bretagne",
  "scripts": {
    "lint": "eslint js/**/*.js --quiet",
    "lint:fix": "eslint js/**/*.js --fix",
    "lint:all": "eslint js/**/*.js",
    "prettier": "prettier --write js/**/*.js css/**/*.css *.html",
    "prettier:check": "prettier --check js/**/*.js css/**/*.css *.html",
    "dev": "npm run serve",
    "dev:check": "npm run lint && npm run prettier:check",
    "build": "npm run build:css && npm run build:js",
    "build:full": "npm run lint:fix && npm run prettier && npm run build:css && npm run build:js",
    "build:css": "npm run build:css:concat && npm run build:css:minify",
    "build:css:concat": "cat css/base.css css/layout.css css/components.css css/poi.css css/responsive.css > dist/styles.css",
    "build:css:minify": "cleancss -o dist/styles.min.css dist/styles.css",
    "build:js": "npm run build:js:concat && npm run build:js:minify",
    "build:js:concat": "cat js/config.js js/security.js js/utils.js js/app.js js/filters.js js/map.js > dist/app.js",
    "build:js:minify": "terser dist/app.js -o dist/app.min.js --compress --mangle",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --coverage --watchAll=false --testTimeout=10000",
    "test:unit": "jest tests/*.test.js --testPathIgnorePatterns=integration",
    "test:integration": "jest tests/integration/*.test.js",
    "test:security": "jest tests/security.test.js",
    "test:utils": "jest tests/utils-clean.test.js",
    "test:filters": "jest tests/filters.test.js",
    "test:map": "jest tests/map.test.js",
    "test:silent": "jest --silent",
    "test:verbose": "jest --verbose",
    "validate": "npm run lint && npm run prettier:check && npm run test:ci",
    "serve": "npx http-server . -p 3000"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "eslint": "^8.57.0",
    "http-server": "^14.1.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.1.2",
    "prettier": "^3.2.5",
    "terser": "^5.44.0"
  },
  "keywords": [
    "bretagne",
    "tourisme",
    "javascript",
    "leaflet",
    "interactive-map"
  ],
  "author": "La Belle Bretagne",
  "license": "MIT",
  "browserslist": [
    "defaults",
    "not IE 11"
  ]
}
